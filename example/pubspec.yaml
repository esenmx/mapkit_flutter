name: mapkit_flutter_example
description: Example app for mapkit_flutter.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.10.0
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter
  mapkit_flutter:
    path: ../

dev_dependencies:
  checks: ^0.3.1
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
